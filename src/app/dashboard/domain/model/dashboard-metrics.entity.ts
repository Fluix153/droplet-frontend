export interface DashboardMetrics {
    totalConsumption: number;     // Litros totales
    flowRate: number;             // Litros por minuto
    temperature: number;          // °C
    activeDevices: number;
}
